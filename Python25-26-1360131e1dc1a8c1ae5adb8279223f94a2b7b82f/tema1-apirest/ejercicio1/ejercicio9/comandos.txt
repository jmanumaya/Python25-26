1. Activar tu entorno virtual

2. Una vez activado el entorno, instalar las librerías dentro de él:

pip install "fastapi[standard]"
pip install "pwdlib[argon2]"
pip install pymongo
pip install "PyJWT==1.7.1"

3. Importante clickar en la conexion de mongodb para que se abra la conexion (en vs code)